# 513Sips Website — CLAUDE.md

> ## 👉 READ `AGENTS.md` IN THIS REPO FIRST
> It is the shared contract for **all** AI tools (Claude Code, Codex, ChatGPT).
> It carries Rule 0 (review recent commits before any new work), the commit
> tagging convention, branch rules, the QA gate rule, writing conventions and
> the 513Sips color palette. This file holds Claude-specific notes only and
> defers to `AGENTS.md` on anything shared.

> Vault path & global context: `~/.claude/CLAUDE.md`
> Project-wide 513Sips context: `../CLAUDE.md`

## This Repo
- **Repo:** `Egady513/513sips-tools` → auto-deploys to **www.513sips.com** on push to `main`
- **Staging:** `Egady513/513Sips-Staging` → egady513.github.io/513Sips-Staging
- **No build step** — push = deploy. Open `index.html` in browser for local preview.

## Key Files
| File | Purpose |
|------|---------|
| `index.html` | Main marketing site (single page) |
| `gallery.html` | Dynamic gallery |
| `tools/calculator.html` | Quote/pricing calculator |
| `blog/index.html` | Blog landing page |
| `nav.js` | Shared nav — single source of truth for all pages |
| `gallery-data.json` | Auto-generated by GitHub Actions — never edit manually |

## Skills: `513sips-brand` → `513sips-qa` → `513sips-deploy` (always in this order)
